# secrets / local env - never commit
.env
.env.*
.dev.vars
*.db
*.db-wal
*.db-shm
bin/
dist/
node_modules/
.svelte-kit/
build/
.wrangler/

graphify-out/

# retired local-only dashboard (SvelteKit, superseded by CF dashboard) - keep on disk, never track
dashboard/
